## **Slide 1 — Title Page**

Good afternoon everyone. My name is Jingcheng Qian, and today I’ll be presenting my research titled *“Data Quality of Generative Images for Object Detection.”*

This study lies at the intersection of computer vision and generative AI. It explores a practical question: whether AI-generated images can genuinely support the training of computer vision systems, especially in cases where real data is rare or costly to collect.

------

## **Slide 2 — The Data Bottleneck**

Object detection is **fundamental** in applications like self-driving, robotics, and security. Yet, these systems are highly data-hungry — they rely on large numbers of labeled images, where each object must be carefully labeled with bounding boxes and class names.

Traditional image augmentation methods like rotation and flipping can improve generalization, but they only modify existing pixels. They cannot create new events, unseen scenes, or rare situations. 

------

## **Slide 3 — The Long-tail Challenge**

In real-world datasets, some events occur often, like clear daytime driving, while rare but safety-critical ones — such as rainstorms, fog, or an ambulance on the road — are less common. This imbalance is known as the long-tail problem.

When models are trained mostly on common cases, they struggle in rare conditions, which can directly affect performance and safety. 

------

## **Slide 4 — Generative Models: A New Opportunity**

Recent advances in generative AI, especially diffusion models, have opened up new ways to generate data. These models learn from large image datasets and can create realistic new scenes from simple text prompts or structural inputs.

This makes them a practical way to expand training data in domains where real images are limited. The question is whether these generated images can actually improve detection performance.

------

## **Slide 5 — Literature Review (Part I)**

Previous research explored several approaches. Traditional augmentation methods like MixUp, CutMix, or Copy-Paste improve diversity but are limited to pixel-level operations — they don’t introduce new object relationships or unseen scene layouts.

Simulation-based datasets, such as those from GTA-V or CARLA, can render labeled images automatically, but they often look artificial, lacking the realism of natural lighting and textures.

Domain adaptation techniques aim to reduce the visual gap between synthetic and real data, yet the differences in texture and style often remain noticeable.

While these methods have made progress, they all share a common limitation — they can’t fully capture the richness and diversity of real-world scenes. This is where modern generative models start to make a difference.

------

## **Slide 6 — Literature Review (Part II)**

Diffusion models have recently surpassed GANs and VAEs in both image quality and controllability. A major innovation is *controllable generation* — the ability to guide how and where objects appear within a scene.

ControlNet enables structural guidance by taking inputs such as edge maps, depth maps, or segmentation layouts, helping the model maintain spatial accuracy and label consistency.

Another important component is  *Low-Rank Adaptation*. Originally developed for large language models, LoRA was designed to fine-tune massive networks efficiently by updating only a small number of parameters instead of retraining the entire model. This idea has since been successfully transferred to diffusion models, allowing them to adapt quickly to new visual domains.

Despite these advances, there is still no systematic study on how elements like ControlNet and LoRA together affect data quality and detection results.

------

## **Slide 7 — Research Objectives **

Building on these ideas, this research focuses on four main objectives.
**quantify the domain gap** between real and synthetic data using multiple visual and semantic metrics.
**evaluate cross-domain robustness**, examining how well models trained with generative data perform when applied to real-world images.
**assess the realism and annotation consistency** of generated samples, ensuring they are both visually convincing and semantically accurate.
**develop best practices** for using generative data effectively in object detection training pipelines.

And additionally, **Identify the contribution of individual components** in the generative augmentation pipeline through **ablation studies**.

------

## **Slide 8 — Research Framework**

The research follows a four-stage framework: **generation**, **filtering**, **training**, and **evaluation**.

In the generation stage, diffusion models enhanced with ControlNet and LoRA are used to produce controllable and adaptive synthetic data. The filtering stage then selects high-quality samples using visual and semantic metrics such as **CLIP**, which measures image–text alignment, and **DINOv2**, which captures semantic similarity between visual features.

Training proceeds with different ratios of real and synthetic data, while the evaluation stage measures both visual quality and detection performance. The feedback from each stage is used to refine the generation process, to make sure improvements are measurable and repeatable.

------

## **Slide 9 — Generation Pipeline**

The generation pipeline is implemented using ComfyUI, a modular and node-based workflow system that supports flexible and reproducible design.

We start with an **original image**, which is first converted into a **Canny edge map**.
That edge map is then fed into **ControlNet**, guiding the model to make a new image with the same layout.

Next, **LoRA** helps adapt the model to different domains — for example, turning a daytime highway scene into a realistic nighttime or foggy version — without the need for full retraining.

Finally, we **reuse the original bounding-box annotations** and apply them directly to the generated image.
This way, we can automatically create realistic and well-labeled data at scale, greatly reducing the need for manual labeling.

Together, these steps make the pipeline both flexible and efficient for object detection training.

------

## **Slide 10 — Training **

After generating the synthetic dataset, we move to the downstream task.
 Here, I focus on two representative object detectors: **YOLO** and **RT-DETR**. These models balance accuracy and real-time efficiency, making them suitable for practical applications like self-driving.

The **training strategy** involves mixing real and synthetic data in different ratios — from entirely real to heavily synthetic — to explore how generative data contributes under various levels of limited data.

The main **objective** of this stage is to quantify the value of synthetic data and identify the most cost-effective balance between real and generated samples. This helps determine how much real data can be replaced without significant performance loss.

## Slide 11 — Evaluation

After training, the models are evaluated through multiple perspectives to understand both task performance and data quality.

For **detection performance**, I use standard object detection metrics such as *mean Average Precision (mAP)* and *AP50*, which directly measure how well the model finds and locates objects. These are task-oriented indicators that reflect real-world performance.

Next, **perceptual quality** is assessed using *FID* and *IS* scores to evaluate the fidelity and diversity of the generated images. Lower FID and higher IS values indicate more realistic and varied results.

To measure **semantic consistency**, I use *CLIP* and *DINOv2* similarity scores, which capture how well the image content aligns with its intended labels or descriptions.

*(Here on the slide, you can see examples of generated images with different semantic scores. Images with higher CLIP and DINOv2 scores appear more faithful to the intended object or scene.)*

Finally, **human evaluation** complements these automated metrics. Participants rate the realism and annotation accuracy of generated samples, providing human insights that pure numerical scores may miss.

Together, these layers of evaluation reveal how visual quality relates to real detection performance, offering a comprehensive understanding of data usefulness.

## Slide 12 — Expected Contributions

*(slight pause, smile)*
 To wrap up today’s presentation, I’d like to highlight the **key contributions** of this research.

*(short pause)*
 **First**, it aims to **clarify the domain gap** between real and synthetic data — and **quantify** how that gap actually impacts object detection accuracy.

*(beat)*
 **Second**, it will **demonstrate the generalization** of detectors trained with synthetic data. In other words, we’ll see **how well these models perform** when applied to **real-world conditions**, especially in those *rare or adverse scenarios (/sɪˈnɑːriəʊ/)* where real data is hard to collect.

*(slight shift in tone)*
 **Third**, the study will **establish evaluation criteria** that link **image diversity, realism, and annotation consistency** to downstream detection performance. This combines both **traditional metrics**, and **semantic verifiers**, giving us a *fuller picture* of data quality.

*(pause, slightly more animated)*
 And **finally**, it will **develop practical guidelines** for integrating generative data into detection pipelines — identifying what really matters, from **prompt design** and **structural control**, to **fine-tuning** and **post-processing**.

*(gentle close)*
 Together, these efforts aim to **bridge the gap** between **generative modeling** and **applied computer vision**, offering both **empirical evidence** and **actionable methods** for making synthetic data truly useful in practice.

*(pause, nod)*
 Thank you.